package hu.ibello.search;

/**
 * All instances of this interface are search modifiers used by <em>ibello</em> element search engine.
 * The instances are generated automatically, there is no need to implement this interface directly.
 * <p>About <em>ibello</em> search engine see {@link hu.ibello.search}.</p>
 * @author Kornél Simon
 *
 */
public interface SearchModifier {
	
}
